{{- if .Values.secrets.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "meridian.service.fullname" (dict "context" . "service" "secrets") }}
  labels:
    {{- include "meridian.service.labels" (dict "context" . "service" "secrets") | nindent 4 }}
spec:
  type: {{ .Values.secrets.service.type }}
  ports:
  - port: {{ .Values.secrets.service.port }}
    targetPort: {{ .Values.secrets.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    {{- include "meridian.service.selectorLabels" (dict "context" . "service" "secrets") | nindent 4 }}
{{- end }}
