{{- if .Values.tasks.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "meridian.service.fullname" (dict "context" . "service" "tasks") }}
  labels:
    {{- include "meridian.service.labels" (dict "context" . "service" "tasks") | nindent 4 }}
spec:
  type: {{ .Values.tasks.service.type }}
  ports:
  - port: {{ .Values.tasks.service.port }}
    targetPort: {{ .Values.tasks.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    {{- include "meridian.service.selectorLabels" (dict "context" . "service" "tasks") | nindent 4 }}
{{- end }}
