{{- if .Values.servers.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "meridian.service.fullname" (dict "context" . "service" "servers") }}
  labels:
    {{- include "meridian.service.labels" (dict "context" . "service" "servers") | nindent 4 }}
spec:
  type: {{ .Values.servers.service.type }}
  ports:
  - port: {{ .Values.servers.service.port }}
    targetPort: {{ .Values.servers.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    {{- include "meridian.service.selectorLabels" (dict "context" . "service" "servers") | nindent 4 }}
{{- end }}
