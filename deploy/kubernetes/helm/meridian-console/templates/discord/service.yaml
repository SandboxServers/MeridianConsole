{{- if .Values.discord.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "meridian.service.fullname" (dict "context" . "service" "discord") }}
  labels:
    {{- include "meridian.service.labels" (dict "context" . "service" "discord") | nindent 4 }}
spec:
  type: {{ .Values.discord.service.type }}
  ports:
  - port: {{ .Values.discord.service.port }}
    targetPort: {{ .Values.discord.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    {{- include "meridian.service.selectorLabels" (dict "context" . "service" "discord") | nindent 4 }}
{{- end }}
