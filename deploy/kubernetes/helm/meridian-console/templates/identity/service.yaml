{{- if .Values.identity.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "meridian.service.fullname" (dict "context" . "service" "identity") }}
  labels:
    {{- include "meridian.service.labels" (dict "context" . "service" "identity") | nindent 4 }}
spec:
  type: {{ .Values.identity.service.type }}
  ports:
  - port: {{ .Values.identity.service.port }}
    targetPort: {{ .Values.identity.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    {{- include "meridian.service.selectorLabels" (dict "context" . "service" "identity") | nindent 4 }}
{{- end }}
