{{- if .Values.nodes.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "meridian.service.fullname" (dict "context" . "service" "nodes") }}
  labels:
    {{- include "meridian.service.labels" (dict "context" . "service" "nodes") | nindent 4 }}
spec:
  type: {{ .Values.nodes.service.type }}
  ports:
  - port: {{ .Values.nodes.service.port }}
    targetPort: {{ .Values.nodes.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    {{- include "meridian.service.selectorLabels" (dict "context" . "service" "nodes") | nindent 4 }}
{{- end }}
