############################################
# PR-Agent / Qodo Merge - .env.example
# Copy to .env and fill in real values.
#
# Notes:
# - For GitHub App mode, your GitHub App webhook should hit:
#     https://<host>/api/v1/github_webhooks
#   (NOT /webhook) :contentReference[oaicite:1]{index=1}
#
# - If your platform can’t handle dots in env var names,
#   replace "." with "__" (double underscore).
#   Example: GITHUB.WEBHOOK_SECRET -> GITHUB__WEBHOOK_SECRET :contentReference[oaicite:2]{index=2}
############################################


############################################
# 1) Choose ONE GitHub auth mode
############################################

# --- Option A: GitHub App (recommended) ---
# Set github deployment type to "app" in config; env override form:
GITHUB__DEPLOYMENT_TYPE=app

# GitHub App ID (from your App settings page)
GITHUB__APP_ID=2549107

# GitHub App webhook secret (random string you generated for the App)
GITHUB__WEBHOOK_SECRET=your_random_webhook_secret_here

# GitHub App private key (the *contents* of the .pem file)
# IMPORTANT: keep it as a single line with \n newlines escaped.
# Example:
# GITHUB__PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nMIIE...\n-----END RSA PRIVATE KEY-----\n"
GITHUB__PRIVATE_KEY=

# Optional (GitHub Enterprise Server):
# GITHUB__BASE_URL="https://github.mycompany.com/api/v3"


# --- Option B: Personal Access Token (PAT) ---
# If you use a PAT, set deployment type to "user"
# and provide a token with appropriate repo permissions.
#
# GITHUB__DEPLOYMENT_TYPE=user
# GITHUB__USER_TOKEN=ghp_your_github_pat_here
#
# Some setups also read GITHUB_TOKEN; if your compose uses that, you can set it too:
# GITHUB_TOKEN=ghp_your_github_pat_here


############################################
# 2) Model Provider Keys (pick what you use)
############################################

# --- OpenRouter (what your current .pr_agent.toml is set up for) ---
OPENROUTER_API_KEY=sk-or-v1_your_openrouter_key_here

# --- OpenAI (optional alternative) ---
# OPENAI_KEY=sk-proj_your_openai_key_here

# --- Anthropic (optional alternative) ---
# ANTHROPIC_API_KEY=sk-ant_your_anthropic_key_here

# --- Google AI Studio / Gemini (optional alternative) ---
# GOOGLE_AI_STUDIO__GEMINI_API_KEY=your_gemini_key_here


############################################
# 3) Optional runtime tweaks
############################################

# Logging verbosity
# LOG_LEVEL=INFO

# If you want PR-Agent to load a specific config file path in the container
# (your compose might already set CONFIG_FILE)
# CONFIG_FILE=/app/configuration.toml

CLOUDFLARE_TUNNEL_TOKEN=...token from Zero Trust tunnel page...
