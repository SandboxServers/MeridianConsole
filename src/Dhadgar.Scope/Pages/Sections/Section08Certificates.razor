<section class="scope-prose">

<h3>Certificate Architecture Overview</h3>
<div class="scope-callout">
<pre class="scope-code">
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         CERTIFICATE MANAGEMENT                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│   EXTERNAL (User-Facing):                                                        │
│   ┌─────────────────┐         ┌─────────────────┐         ┌─────────────────┐   │
│   │  Let's Encrypt  │◄───────►│  Cloudflare API │◄───────►│  cert-manager   │   │
│   │  (ACME CA)      │         │  (DNS-01)       │         │  (Ingress only) │   │
│   └─────────────────┘         └─────────────────┘         └─────────────────┘   │
│                                                                                  │
│   INTERNAL (Pod-to-Pod):                                                         │
│   ┌─────────────────────────────────────────────────────────────────────────┐   │
│   │                         CILIUM SERVICE MESH                              │   │
│   │   • Automatic mTLS between all pods                                      │   │
│   │   • No manual cert management                                            │   │
│   │   • Covers: services, PostgreSQL, RabbitMQ, Redis                        │   │
│   └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│   AGENT CERTIFICATES:                                                            │
│   ┌─────────────────────────────────────────────────────────────────────────┐   │
│   │                    NODES SERVICE (Agent Cert Manager)                    │   │
│   │                                                                          │   │
│   │  Bootstrap: Token → Register → Issue cert → Establish mTLS              │   │
│   │  Renewal: Automated at 45 days before expiry                             │   │
│   │  DNS: agent-{id}.nodes.meridianconsole.com                              │   │
│   └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
        </pre>
</div>
<h3>Certificate Types</h3>
<table class="scope-table">
<thead>
<tr>
<th>Type</th>
<th>Scope</th>
<th>Manager</th>
<th>Renewal</th>
</tr>
</thead>
<tbody>
<tr><td>Ingress (User-facing)</td><td>*.meridianconsole.com</td><td>cert-manager</td><td>45 days (automated)</td></tr>
<tr><td>Internal Pod-to-Pod</td><td>All cluster services</td><td>Cilium</td><td>Automatic (mesh)</td></tr>
<tr><td>Agent mTLS</td><td>agent-{id}.nodes.*</td><td>Nodes Service</td><td>45 days (automated)</td></tr>
</tbody>
</table>
<h3>Agent Bootstrap Flow</h3>
<div class="scope-callout">
<pre class="scope-code">
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           AGENT BOOTSTRAP SEQUENCE                               │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  1. ADMIN GENERATES TOKEN                                                        │
│     ┌─────────┐     POST /nodes/tokens      ┌─────────────┐                     │
│     │  Admin  │ ───────────────────────────►│   Nodes     │                     │
│     │   UI    │◄─────────────────────────── │   :5040     │                     │
│     └─────────┘     { token: "xxx" }        └─────────────┘                     │
│                     (15 min expiry)                                              │
│                                                                                  │
│  2. AGENT REGISTERS                                                              │
│     ┌─────────┐     POST /nodes/register    ┌─────────────┐                     │
│     │  Agent  │ ───────────────────────────►│   Nodes     │                     │
│     │  (new)  │     { token: "xxx" }        │   :5040     │                     │
│     └─────────┘                             └──────┬──────┘                     │
│                                                    │                             │
│  3. NODES SERVICE PROVISIONS CERT                  │                             │
│                     ┌──────────────────────────────┘                             │
│                     ▼                                                            │
│     ┌─────────────────────────────────────────────────────────────────────────┐ │
│     │  • Create DNS entry: agent-{id}.nodes.meridianconsole.com              │ │
│     │  • Request cert from Let's Encrypt (via Cloudflare API DNS-01)         │ │
│     │  • Store cert metadata in nodes_db                                      │ │
│     └─────────────────────────────────────────────────────────────────────────┘ │
│                     │                                                            │
│  4. PUSH CERT TO AGENT                                                           │
│     ┌─────────┐     cert + private key      ┌─────────────┐                     │
│     │  Agent  │◄─────────────────────────── │   Nodes     │                     │
│     │  (new)  │     (over initial TLS)      │   :5040     │                     │
│     └─────────┘                             └─────────────┘                     │
│                                                                                  │
│  5. ESTABLISH mTLS                                                               │
│     ┌─────────┐     mTLS connection         ┌─────────────┐                     │
│     │  Agent  │◄═══════════════════════════►│   Nodes     │                     │
│     │  (reg)  │     (ongoing comms)         │   :5040     │                     │
│     └─────────┘                             └─────────────┘                     │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
        </pre>
</div>
<h3>DNS-01 Challenge Flow</h3>
<table class="scope-table">
<thead>
<tr>
<th>Step</th>
<th>Action</th>
<th>Component</th>
</tr>
</thead>
<tbody>
<tr><td>1</td><td>Request new certificate order</td><td>→ Let's Encrypt ACME</td></tr>
<tr><td>2</td><td>Create TXT record via API</td><td>→ Cloudflare API</td></tr>
<tr><td>3</td><td>Validate challenge</td><td>→ Let's Encrypt ACME</td></tr>
<tr><td>4</td><td>Delete TXT record</td><td>→ Cloudflare API</td></tr>
<tr><td>5</td><td>Store/push certificate</td><td>→ K8s Secret / Agent</td></tr>
</tbody>
</table>
<h3>Renewal Policy</h3>
<div>
<p>✅ Automated Renewal</p>
<ul>
<li>• <strong>Validity:</strong> 90 days (Let's Encrypt standard)</li>
<li>• <strong>Renewal trigger:</strong> 45 days before expiry (50% lifetime)</li>
<li>• <strong>Automation:</strong> Fully automated — no manual intervention</li>
<li>• <strong>Alerting:</strong> Alert if renewal fails 30 days before expiry</li>
</ul>
</div>
<h3>Internal Services (Cilium)</h3>
<div>
<p>🔒 Pod-to-Pod Encryption</p>
<p>Cilium service mesh handles all internal mTLS automatically:</p>
<ul>
<li>• All 12 control plane services</li>
<li>• PostgreSQL connections (Platform DB, User DB)</li>
<li>• RabbitMQ connections</li>
<li>• Redis connections</li>
<li>• Prometheus/Loki scraping</li>
</ul>
<p>No manual certificate management required for internal services.</p>
</div>
</section>
