# Production environment variables for ShoppingCart
# These are baked into the build at compile time by Astro

# Gateway URL - where BetterAuth and Identity APIs are hosted
PUBLIC_GATEWAY_URL=https://dev.meridianconsole.com
