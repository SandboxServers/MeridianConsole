<Project Sdk="Microsoft.NET.Sdk">
  <!--
    Placeholder test project for Dhadgar.ShoppingCart.
    The actual ShoppingCart is a Node.js/Astro project - this test project exists
    for solution structure consistency. Real tests should use Vitest/Jest.
  -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
    <Nullable>enable</Nullable>
    <AssemblyName>Dhadgar.ShoppingCart.Tests</AssemblyName>
    <RootNamespace>Dhadgar.ShoppingCart.Tests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" PrivateAssets="all" />
  </ItemGroup>

  <!-- No ProjectReference - ShoppingCart is a Node.js project that doesn't produce a .NET assembly -->
</Project>
