<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <IsPackable>false</IsPackable>
    <Nullable>enable</Nullable>
    <AssemblyName>Dhadgar.Scope.Tests</AssemblyName>
    <RootNamespace>Dhadgar.Scope.Tests</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" PrivateAssets="all" />
  </ItemGroup>

  <!--
    Note: Dhadgar.Scope is now an Astro/Node.js project, not a .NET project.
    Tests for the Scope site should be done with Playwright or similar E2E testing.
    This test project validates the project structure and build artifacts.
  -->
</Project>
